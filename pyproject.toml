[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cultural-bias-red-team"
version = "0.1.0"
description = "Synthetic red-team probe generation for cultural bias evaluation"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.13"
dependencies = [
    "data-designer>=0.1.0",
    "pydantic>=2.0",
    "numpy>=2.0",
    "pandas>=2.2",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[project.urls]
Repository = "https://github.com/williamcaban/cultural-bias-red-team"
Issues = "https://github.com/williamcaban/cultural-bias-red-team/issues"

[tool.hatch.build.targets.wheel]
packages = ["specs"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["specs"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "ruff>=0.6",
]
